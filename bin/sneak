#!/usr/bin/env node
require('colors');

var Sneak = require('..')
  , args = process.argv.slice(2)
  , cmd = args[0]
  , path = require('path');

switch (cmd) {
case "render":
  var path = path.resolve(process.cwd(), args[1]);
  Sneak.renderFile(path, 'utf8',
    function (err, response){
      if (err) {
        console.error(err);
      } else {
        console.log(response);
      }
    }
  )
}
