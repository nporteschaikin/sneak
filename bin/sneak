#!/usr/bin/env node
require('colors');

var Sneak = require('..')
  , args = process.argv.slice(2)
  , cmd = args[0]
  , path = require('path');

switch (cmd) {
case "render":
  Sneak.renderFile(path.resolve(process.cwd(), args[1]));
}
